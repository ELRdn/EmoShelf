# EmoShelf — Landing page design

更新: 2026-09-06 / 壁打ちで合意した方針とローカルモックの設計正本。

## 1. 目的とコンセプト

**いつもの絵文字を、あなたの棚に。 / Your favorites. Within reach.**

幅広いWindowsユーザーに「よく使う絵文字を繰り返し探す手間を減らせる」と伝える。Xでの紹介から訪問し、棚を触って便利さを理解し、Xで公開情報を確認する流れを作る。日本語・英語を同時に用意する。

明るい背景にダークな棚を置く。絵文字には遊び心、操作には精密さを持たせる。無料・オープンソース、Windows 11対応、アカウント不要を簡潔に伝える。料金の将来保証、未検証の速度数値、架空のレビューや利用者数は掲載しない。

## 2. 参考と独自性

- https://www.edgedrop.app/ — 最優先の参考。太く丸い文字、余白、触感のある遊び心、短い注釈と操作の説明を取り入れる。素材・文言・特徴的な曲線レイアウトを複製しない。
- https://openai.com/index/gpt-6-astra/ — 主役に視線を集める画面構成を参考にする。
- https://deepseek.com/harness/en/ — 整理された情報と奥行きのバランスを参考にする。

EmoShelfの特徴は、ヒーローの絵文字が「飾り」だけで終わらず、触れる棚の中で使えること。大規模な背景グラデーション、常時動く粒子、スクロールの強制制御、読み込みを待たせる導入演出は採用しない。

## 3. ビジュアル仕様

| 役割 | 値 / 使い方 |
| --- | --- |
| ページ背景 | `#F4F4F6`、面は `#FAFAFB` |
| 本文 / 補助文 | `#18181B` / `#62616C` |
| ライト面の紫 | `#7467E8`、CTA・リンク・フォーカス |
| 棚 | `#111113`、カード `#222226`、文字 `#F5F5F7` |
| ダーク面の紫 | `#8B7CFF`、選択中の棚・選択状態 |
| 黄色 | `#FFC857`、お気に入り・選択完了の小さな強調 |
| 境界線 | ライト `#E3E3E7`、ダーク `#373740` |

色の意味はDESIGN.mdに従う。Purple tells you where you are. Yellow tells you what you love. 黄色を主要CTAや白地の小さな文字に使わない。

見出しはArial Rounded MT Bold / Hiragino Maru Gothic ProN / Yu Gothicの太字を使い、本文はSegoe UI / Yu Gothic / Meiryo。外部フォント通信はしない。初期モックに対してPCの文字・棚・ボタンを約1.2倍に拡大する。日本語ヒーロー見出しは画面幅に応じて54〜88.8px、英語は96px、ヒーロー説明文は18px。モバイルの固定寸法は約1.1倍を目安とし、見出しは画面幅に収める。欧文見出しは字間を詰め、日本語は過度に詰めない。小ラベルとキーボード表示には等幅フォントを使う。

最大幅1392px、左右余白PC48px・モバイル22px。棚は最大864px、メッセージプレビューは最大666px。段落は読みやすい幅に制限し、セクション間の縦余白は一律に拡大しない。1400px以下では棚の注釈を上へ移し、380px以下ではナビを2段にする。棚に控えめな傾きと複数の薄い影を与え、ボタンは丸く、本文は整然と配置する。棚の外の絵文字は最大4個、装飾として読み上げ対象から外す。

## 4. ページ構成とコピー

1. ナビ: ロゴ、使い方 / How it works、よくある質問 / FAQ、JA / EN、GitHub。
2. ヒーロー: 「いつもの絵文字を、あなたの棚に。」/ “Your favorites. Within reach.”。説明は「好きな絵文字を並べて、Alt + Eで呼び出す。探す時間を、伝える時間に。」/ “Keep the emoji you love on your shelf. Press Alt + E, pick one, and get back to the conversation.”。主CTAは「Xで公開情報を見る」/ “Follow on X”、副CTAは「棚を試す」/ “Try the shelf”。
3. 触れる棚: My Shelf / Reactions / Workの3つのサンプル棚。絵文字選択でサンプルメッセージ欄に反映。右側の選択プレビューで結果も示す。
4. 使い方: 「並べる。呼び出す。伝わる。」/ “Make it yours. Make it quick.”。棚を用意→Alt + Eで呼び出す→貼り付けの3段階を短く説明。
5. 実アプリ: リポジトリの実スクリーンショットと、Board・日本語/英語検索・画像対応・ローカル保存を紹介。ミニデモと実アプリを区別する。
6. FAQ: 対応OS、無料、Windows標準入力との違い、データの保存、公開時期を日英で提供。
7. 終端CTA: 「あなたの棚、もうすぐ。」/ “A little shelf. A lot more you.”、X / GitHubへの導線。
8. フッター: 無料・Apache-2.0、Twemoji帰属、プライバシーの要約。

## 5. デモとインタラクション

- デモはブラウザ内のサンプル。OSのショートカット登録、クリップボードアクセス、他アプリへの送信は行わない。Alt + Eは実アプリの説明用キー表示。
- 棚切り替え、絵文字クリック、キーボードのTab / Enter / Space、リセットが動作する。選択された絵文字1個がメッセージ末尾に付く。履歴や個人入力を収集しない。
- マウスホバーで2〜4px持ち上がり、選択時だけ短い反応。Reduced Motionではアニメーション・傾き・スムーズスクロールを無効にする。
- サンプルの棚には名前付きのボタンを用い、選択はaria-pressedで伝える。結果はaria-liveで読み上げる。
- 840px以下では1列。装飾と詳細プレビューを減らし、絵文字を44px以上のタッチ対象にする。ページ内の全操作はタッチ対応。

## 6. CTAと公開前後

公開前は「公開準備中 / Coming soon」を表示する。Xが主導線、GitHubが副導線。URLはユーザー指定のX `https://x.com/ELRadn`、リポジトリ `https://github.com/ELRdn/EmoShelf` を `app/lp/src/config.ts` で管理する。作者プロフィール `https://github.com/ELRdn` はフッターに配置する。未設定なら外部リンクを捏造せず、ボタン操作で「リンクは公開準備中」と通知する。Xフォローで確実に通知が届くとは表現しない。

正式公開時は署名済み配布物の確認後、主CTAをWindows用ダウンロードに変更する。未署名RCを正式版として誘導しない。URL、公開日、署名完了はこのモックの作成によって確定しない。

## 7. 技術・マーケティング・受け入れ条件

提案・採用: **React 19 + TypeScript + Vite**。既存のapp依存を再利用し、`app/lp/`を独立したVite rootとする。追加UIライブラリ・依存更新は不要。Tauriアプリの入口・ビルドとは分離する。LP本体と挙動はTS/TSXで作る。

ローカル実行はappで `pnpm lp:dev`、型検査とビルドは `pnpm lp:build`、成果物は `app/lp/dist/`。初期言語は日本語。切り替えでhtml langとページタイトルも更新する。

モックはnoindex。公開時には公開URL、canonical、hreflang、日英それぞれの静的メタ情報、1200×630 OG画像を追加し、noindexを解除する。OGは短いコピー＋紫の棚＋黄色の小さなアクセントで作る。モックにアクセス解析・Cookie・フォームは導入しない。将来の計測はページ訪問→デモ利用→外部CTAクリックを候補に別途決める。外部クリックを実際のフォロー獲得数として扱わない。

受け入れ: TypeScript/production build成功、日英切り替え、棚切り替え、絵文字選択・リセット、未設定リンク通知、FAQ、PCと390px幅の表示、横スクロールなし、キーボードフォーカス、Reduced Motionを確認する。実アプリとLPの機能を混同させない。

## 8. 仮公開運用（2026-09-06承認）

ユーザーの公開指示により、GitHub Pagesへ仮公開する。公開URLは `https://elrdn.github.io/EmoShelf/`。GitHub Actionsの `LP Pages` がmainのLP関連変更をビルドし、`app/lp/dist` のみを配信する。PRではビルドのみ、手動実行はmainから再公開できる。

仮公開中はnoindex、「公開準備中」、X / GitHubへの導線を維持する。noindexは閲覧制限ではない。正式公開用SEO・OGと署名済みダウンロード導線への変更は別の段階として扱う。
